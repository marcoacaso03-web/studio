
{
  "entities": {
    "ScoutPlayer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ScoutPlayer",
      "type": "object",
      "description": "Represents a player identified and tracked by the scouting module, including their details and associated categories. This entity is distinct from the core 'Player' entity, focusing specifically on external talents scouted by the user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ScoutPlayer entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the scouted player."
        },
        "role": {
          "type": "string",
          "description": "The primary role or position of the scouted player (e.g., 'Attacker', 'Midfielder', 'Defender')."
        },
        "currentTeam": {
          "type": "string",
          "description": "The name of the team the scouted player currently plays for outside of the user's managed team."
        },
        "notes": {
          "type": "string",
          "description": "Additional observations or comments about the scouted player, providing qualitative insights."
        },
        "categoryIds": {
          "type": "array",
          "description": "References to ScoutCategories this player belongs to, allowing for classification and filtering. (Relationship: ScoutCategory N:N ScoutPlayer)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "role",
        "currentTeam",
        "categoryIds"
      ]
    },
    "ScoutCategory": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ScoutCategory",
      "type": "object",
      "description": "Represents a custom category or tag created by the user to classify and filter scouted players, often displayed as colored badges.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ScoutCategory entity."
        },
        "name": {
          "type": "string",
          "description": "The custom name of the scout category or tag (e.g., 'Promessa', 'Fisico', 'Piede Sinistro')."
        },
        "colorHex": {
          "type": "string",
          "description": "Hexadecimal color code for the badge associated with this category, used for UI representation (e.g., '#FF8F00')."
        }
      },
      "required": [
        "id",
        "name",
        "colorHex"
      ]
    },
    "TrainingSession": {
      "title": "TrainingSession",
      "type": "object",
      "description": "Represents a single training session.",
      "properties": {
        "id": { "type": "string" },
        "index": { "type": "number" },
        "date": { "type": "string", "format": "date-time" },
        "notes": { "type": "string" },
        "seasonId": { "type": "string" }
      },
      "required": ["id", "index", "date", "seasonId"]
    },
    "TrainingAttendance": {
      "title": "TrainingAttendance",
      "type": "object",
      "description": "Represents a player's attendance status for a training session.",
      "properties": {
        "playerId": { "type": "string" },
        "status": { "type": "string", "enum": ["presente", "ritardo", "assente"] }
      },
      "required": ["playerId", "status"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/scoutPlayers/{scoutPlayerId}",
        "definition": {
          "entityName": "ScoutPlayer",
          "schema": {
            "$ref": "#/backend/entities/ScoutPlayer"
          },
          "description": "Stores individual scouted player data."
        }
      },
      {
        "path": "/users/{userId}/scoutCategories/{scoutCategoryId}",
        "definition": {
          "entityName": "ScoutCategory",
          "schema": {
            "$ref": "#/backend/entities/ScoutCategory"
          },
          "description": "Stores custom categories (tags)."
        }
      },
      {
        "path": "/users/{userId}/trainingSessions/{sessionId}",
        "definition": {
          "entityName": "TrainingSession",
          "schema": {
            "$ref": "#/backend/entities/TrainingSession"
          },
          "description": "Stores individual training sessions."
        }
      },
      {
        "path": "/users/{userId}/trainingSessions/{sessionId}/attendance/{playerId}",
        "definition": {
          "entityName": "TrainingAttendance",
          "schema": {
            "$ref": "#/backend/entities/TrainingAttendance"
          },
          "description": "Stores attendance for a specific training session."
        }
      }
    ],
    "reasoning": "The training module uses the same isolation pattern as Scout, nesting sessions and their respective attendance records under the user document."
  }
}
